import { FocusInvalid } from './focus-invalid';

describe('FocusInvalid', () => {
  it('should create an instance', () => {
    const directive = new FocusInvalid();
    expect(directive).toBeTruthy();
  });
});
